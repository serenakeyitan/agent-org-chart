# Chart index

Available org charts in this catalog. Agents: if the user did not name an `id`, list these and ask which to import. See [`../PROTOCOL.md`](../PROTOCOL.md).

| id | title | one-liner |
| --- | --- | --- |
| [`company-os`](./company-os/) | Company OS | Company OS livestream pattern: Project Manager + four specialists + Active Bots |

**Notes**
- Charts in this catalog are verified patterns from official livestream sources only.
- Prefer official livestream or broadcast URLs as sources; avoid third-party note threads with personal attribution.

Add more under `charts/<id>/` (copy `company-os` as a template), then append a row here.
