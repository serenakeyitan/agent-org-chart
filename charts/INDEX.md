# Chart index

Available org charts in this catalog. Agents: if the user did not name an `id`, list these and ask which to import. See [`../PROTOCOL.md`](../PROTOCOL.md).

| id | title | one-liner |
| --- | --- | --- |
| [`company-os`](./company-os/) | Company OS | Orchestrator + research/product/people/performance specialists and an Active Bots channel |

Add more under `charts/<id>/` (copy `company-os` as a template), then append a row here.
